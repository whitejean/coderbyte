def VowelCount(str)

  # code goes here
  x = str.count "aeiou"
  return x 
         
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
VowelCount(STDIN.gets)           
